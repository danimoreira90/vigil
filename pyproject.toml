[project]
name = "vigil"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "kaggle>=2.2.1",
    "pandas>=3.0.3",
    "pyarrow>=21.0.0",
    "pytest>=8.4.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
