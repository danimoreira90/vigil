"""System 2 retrieval — embeddings + vector search + lexical + hybrid.

Package marker only; import from the real modules (CS-8). Public surface is
intentionally split across files so the notebook and tests use the function
they need, not a barrel.
"""
